{
  "name": "youtube-influencer-analyzer",
  "version": "1.0.0",
  "description": "YouTube Influencer Analysis Chrome Extension with Full Stack Backend",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run frontend\"",
    "server": "cd server && npm run dev",
    "frontend": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "build-extension": "cd extension && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../frontend && npm install && cd ../extension && npm install"
  },
  "keywords": ["youtube", "influencer", "analytics", "chrome-extension"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}